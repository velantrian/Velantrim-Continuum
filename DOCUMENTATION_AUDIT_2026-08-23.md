# Documentation Audit — 2026-08-23

**Scope:** accuracy/currency (точность и актуальность) and completeness/structure (полнота и структура) of this repository's documentation (`*.md`, README, `docs/`), assessed against a snapshot of the default branch on 2026-08-23. This is a documentation snapshot audit, not a code-quality or security review, and does not cover unmerged branches (several `docs/*` branches exist upstream that are not part of this snapshot).

## Overall health assessment

**Fair.** The "authoritative state" chain — `project-state.json`, `STATUS.md`, `docs/ai/CURRENT_STATE.md`, `docs/ai/README.md`, and the approval record `experiments/e0/approval/human-reference-approval.v0.2.json` — is unusually rigorous: every hash, PR number, commit SHA, and decision count cross-checked matches byte-for-byte, the EN/RU README and RESEARCH_OVERVIEW pairs are fully parallel, and all internal markdown links resolve with no stub/empty files or stray TODO/TBD markers anywhere in the 20 files. That discipline is undercut by one genuine, high-impact defect: `docs/ai/AUDIT_AND_FUTURE_WORK.md` — itself listed as required "authoritative" reading — presents a two-day-stale snapshot that flatly contradicts the current state recorded everywhere else, and the `docs/research/` directory has a "Research Index" that indexes almost none of its own contents. Fix those two and this would be a strong "good."

## Findings

1. **docs/ai/AUDIT_AND_FUTURE_WORK.md** | accuracy | high | The ledger's "current" evidence section directly contradicts the actual, more recent project state, describing the human-reference gate as still open and unapproved when it has in fact been closed. | Dated "Last live reconciliation: 2026-08-17," it states IDPS-FW-001 is `BLOCKED / HUMAN_ACTION_REQUIRED` with "Issue #9 remains open, all checklist decisions remain pending" and `HUMAN_APPROVED: NO`. But `project-state.json` records `"status": "HUMAN_APPROVED"`, `"gate_status": "CLOSED"`, `"accepted_decisions": 14`, reconciled `2026-08-21T19:12:00Z`.

2. **docs/research/README.md** | completeness | high | The "Research Index" links only 1 of the 8 files in `docs/research/`, leaving most research documents — including one added in the repo's newest commit — undiscoverable from any index or cross-reference. | The index's only content link is `[IDPS_EXPERIMENT_0_PREREGISTRATION.md]`; `E0_HARD_FAIL_LAW_V0_2.md`, `EITI_CONTINUITY_RESEARCH_INPUT.md`, `COGNITIVE_AI_COMPARATIVE_BASELINES_2026_08_22.md`, and `SMART_CONTEXT_HANDOFF_MAPPING.md` have zero inbound references anywhere else in the repo.

3. **experiments/e0/review/ISSUE_9_HUMAN_REVIEW_PROTOCOL.md** | accuracy | medium | This file, linked directly from `AGENTS.md`, `docs/ai/README.md`, and `docs/ai/CURRENT_STATE.md`, still shows a "requires fresh review" banner and all 14 rows as `PENDING`, with no note that every row was resolved `ACCEPT` on 2026-08-21. | Likely intentional as a hash-bound reviewed snapshot, but it offers no in-file pointer to the resolution.

4. **docs/research/E0_HARD_FAIL_LAW_V0_2.md** | completeness | low | The doc's status line cites an identifier, "OD-06," that is defined nowhere else in the repository.

5. **experiments/e0/README.md** | completeness | low | No markdown document explains how to install dependencies or run the Experiment 0 harness/tests locally — those commands exist only in the CI workflow YAML (`.github/workflows/e0-harness.yml`).

6. **docs/research/E0_CORRESPONDENCE_LAW_V0_2.md** | structure | low | The superseded v0.2 law carries no "superseded by v0.3" pointer, and neither version is linked from the research index, so a reader landing on v0.2 directly has no signal it's outdated.

7. **repo-wide** | structure | low | No LICENSE file exists and no doc mentions licensing terms, despite the repo shipping reusable Python scripts/schemas (`scripts/`, `experiments/e0/schema/`).

8. **docs/research/COGNITIVE_AI_COMPARATIVE_BASELINES_2026_08_22.md** | structure | low | Its date-suffixed filename is a one-off naming convention not used anywhere else in the doc set (siblings use `_V0_2`/`_V0_3` semantic versioning or no version marker at all).

---
*Generated by an automated documentation audit (Claude Code).*
